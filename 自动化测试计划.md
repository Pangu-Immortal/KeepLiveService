# KeepLiveService 自动化测试计划

> 日期：2026-05-28
> 范围：全量保活策略优化、健康巡检、VPN 授权、MediaBrowser、C++ startActivity 编排

## 1. 本地构建验证

| 项目 | 命令 | 预期 |
|------|------|------|
| framework Debug AAR | `./gradlew :framework:assembleDebug` | Kotlin、Manifest、CMake、四架构 native 均构建成功 |
| app Debug APK | `./gradlew :app:assembleDebug` | 示例应用可安装 |
| 发布前检查 | `./gradlew :framework:assembleRelease` | release AAR、sources、javadoc 任务可执行 |
| Rust 工具链检查 | `./gradlew :framework:checkFwRustToolchain` | Cargo 与 NDK 四架构 linker 可用 |
| Rust Native 可选打包 | `./gradlew :framework:assembleRelease -PfwBuildRust=true` | 生成并打包 4 ABI `libfw_rust.so` |

## 2. Native startActivity 验证

| 场景 | 调用 | 预期 |
|------|------|------|
| 默认入口 | `FwStart.start(context, intent)` | 只执行可落地策略，返回命中策略或 `NO_STRATEGY_SUCCEEDED` |
| 全量审计 | `FwStart.startAuditAll(context, intent)` | 研究路径进入日志，特权/漏洞型路径返回安全跳过 |
| Activity Context | Activity 内调用 | 优先验证 `moveTaskToFront`、直启、`startActivityForResult` |
| Application Context | Application 内调用 | 自动添加 `NEW_TASK`，兜底走 PendingIntent / 双 Intent / NEW_TASK |
| Android 14-16 | API 34/35/36 真机或模拟器 | PendingIntent BAL sender/creator-side options 不崩溃 |

## 3. 健康巡检验证

| 场景 | 步骤 | 预期 |
|------|------|------|
| 初始化后巡检 | `Fw.init(this)` 后调用 `Fw.check()` | 返回 `FwHealthReport`，日志输出每项策略状态 |
| Job 被取消 | 手动取消 Job 后调用 `Fw.check()` | 自动重新调度 Job，`recovered=true` |
| Work 被取消 | 手动取消 WorkManager 后调用 `Fw.check()` | 自动重新入队 Work |
| Alarm 被取消 | 手动取消闹钟后调用 `Fw.check()` | 自动重新调度，缺少精确闹钟时降级提示 |
| Native 不可用 | 移除或屏蔽 native so 后巡检 | Native 项标记为异常，不影响其他策略 |

## 4. 用户授权类策略验证

| 策略 | 步骤 | 预期 |
|------|------|------|
| VPN | 调用 `FwVpnService.prepareIntent(activity)` 并完成系统授权 | 授权前不启动；授权后建立占位 VPN，状态栏可见，`Fw.check()` 标记运行 |
| CompanionDevice | 完成 BLE 设备关联 | 已关联设备数量大于 0 时进入运行态 |
| 设备管理员 | 按企业设备流程激活 | `Fw.check()` 标记运行，否则提示需要权限 |
| 小组件 | 添加桌面小组件 | `Fw.check()` 显示已添加数量 |
| 快捷磁贴 | 添加通知栏磁贴 | 用户触发后日志出现 TileService 生命周期 |

## 5. 真机矩阵

| 系统 | 重点 |
|------|------|
| Android 8-9 | 前台服务、Job、Alarm、Native daemon、1 像素 Activity |
| Android 10-11 | 后台启动 Activity 限制、Binder 21-30 分支、MediaRoute |
| Android 12-13 | 精确闹钟权限、蓝牙权限、通知权限、CompanionDevice |
| Android 14 | 前台服务类型、specialUse subtype、PendingIntent BAL |
| Android 15 | 16KB 页面大小、前台服务限制、构建产物 ELF 对齐 |
| Android 16 | PendingIntent sender/creator-side 模式和可见时允许分支 |

## 6. 日志验收

```bash
adb logcat | grep -E "Fw|FwStart|FwHealth|FwVpnService|FwMediaBrowser"
```

验收重点：
- `FwHealthReport` 包含健康策略数、问题策略数和补偿结果。
- `FwStart` 每个策略都有开始、成功、失败或跳过日志。
- VPN 未授权时只提示授权，不尝试建立隧道。
- MediaBrowserService 创建、连接、销毁都有状态打点。

## 7. Rust 重构第一阶段验证记录

| 验证项 | 命令/方式 | 结果 |
|--------|-----------|------|
| 默认完整构建 | `./gradlew clean build` | 通过 |
| AndroidTest 构建 | `./gradlew :framework:assembleAndroidTest :app:assembleAndroidTest` | 通过 |
| Release Lint | `./gradlew :framework:lintRelease :app:lintRelease` | 通过 |
| Rust 工具链 | `./gradlew :framework:checkFwRustToolchain -PfwCargoPath=/opt/homebrew/opt/rustup/bin/cargo` | 通过 |
| Rust Release 打包 | `./gradlew :framework:assembleRelease -PfwBuildRust=true -PfwCargoPath=/opt/homebrew/opt/rustup/bin/cargo` | 通过 |
| AAR 内容 | `unzip -l framework/build/outputs/aar/framework-release.aar` | 4 ABI 均包含 `libfw_rust.so` |
| Rust so 符号 | `llvm-nm -D --defined-only libfw_rust.so` | 动态导出仅 `JNI_OnLoad` |
| 16KB 对齐 | `llvm-readelf -l libfw_rust.so` | 4 ABI LOAD Align 均为 `0x4000` |
| 默认 C++ 回退设备侧测试 | `./gradlew :framework:connectedAndroidTest --rerun-tasks` | 通过；Rust 专属用例在未打包 Rust so 时受控跳过，公共 MediaRoute 入口走 C++ 回退 |

## 8. Rust 进程信息迁移验证记录

| 验证项 | 命令/方式 | 预期 |
|--------|-----------|------|
| Rust 代码格式 | `cargo fmt --manifest-path framework/src/main/rust/fw_rust/Cargo.toml -- --check` | 通过 |
| Rust 4 ABI 编译 | `./gradlew :framework:assembleRelease -PfwBuildRust=true -PfwCargoPath=/opt/homebrew/opt/rustup/bin/cargo` | 通过 |
| 默认回退构建 | `./gradlew :framework:assembleRelease` | 不依赖 Rust so，构建通过 |
| 健康巡检回退 | 默认 AAR 不含 `libfw_rust.so` 时调用 `Fw.check()` | 只输出调试日志，不影响 C++ Native 巡检 |
| 符号防护 | `llvm-nm -D --defined-only libfw_rust.so` | 只导出 `JNI_OnLoad` |

本轮补充验证：`./gradlew clean build` 已单独重跑通过；`./gradlew :framework:assembleAndroidTest :app:assembleAndroidTest -PfwBuildRust=true -PfwCargoPath=/opt/homebrew/opt/rustup/bin/cargo` 已通过；Android 16 模拟器已执行默认 C++ 回退矩阵和 Rust 打包矩阵。

## 9. Rust MediaRoute 迁移与模拟器验证记录

| 验证项 | 命令/方式 | 预期 |
|--------|-----------|------|
| Rust MediaRoute 编译 | `./gradlew :framework:assembleRelease -PfwBuildRust=true -PfwCargoPath=/opt/homebrew/opt/rustup/bin/cargo` | 通过且无 Rust 编译告警 |
| 默认 C++ 回退 | `./gradlew :framework:assembleRelease` | 不依赖 Rust so，构建通过 |
| 符号防护 | `llvm-nm -D --defined-only libfw_rust.so` | 只导出 `JNI_OnLoad` |
| 模拟器启动 | `android emulator start medium_phone` 失败后，使用 `emulator -avd medium_phone -no-snapshot-load -no-snapshot-save -no-boot-anim -no-window` | Android 16 / arm64-v8a 设备进入 `device` 状态 |
| App 安装 | `./gradlew :app:installDebug -PfwBuildRust=true -PfwCargoPath=/opt/homebrew/opt/rustup/bin/cargo` | Debug APK 安装成功，APK 内包含 4 ABI `libfw_rust.so` |
| 基础运行 | `adb shell am start -n com.google.services/.SplashActivity` | App 可启动，Rust MediaRoute 初始化成功，logcat 无 JNI/native/AndroidRuntime 崩溃 |
| 默认 C++ 回退设备侧测试 | `./gradlew :app:connectedAndroidTest --rerun-tasks` 与 `./gradlew :framework:connectedAndroidTest --rerun-tasks` | App 5 个 startActivity 用例通过；framework 公共入口用例通过，Rust 专属用例在默认构建受控跳过 |
| Rust 打包设备侧测试 | `./gradlew connectedAndroidTest --rerun-tasks -PfwBuildRust=true -PfwCargoPath=/opt/homebrew/opt/rustup/bin/cargo` | app 5 tests / 0 failures / 0 skipped；framework 3 tests / 0 failures / 0 skipped |
| 崩溃日志扫描 | `adb -s emulator-5554 logcat -d | rg -i 'AndroidRuntime|FATAL EXCEPTION|UnsatisfiedLinkError|JNI DETECTED ERROR|Fatal signal|SIGSEGV|SIGABRT'` | 无应用崩溃、JNI 错误或 native crash |
| 默认产物边界 | 默认 `:framework:assembleRelease` 与 `:app:assembleDebug` 后检查 APK/AAR | 未包含 `libfw_rust.so`，不会误打包旧 Rust so |
| Rust 产物边界 | 带 `-PfwBuildRust=true` 构建后检查 APK/AAR | APK/AAR 均包含 4 ABI `libfw_rust.so` |

本轮补充验证：`cargo fmt --check`、`./gradlew clean build`、`./gradlew :framework:assembleAndroidTest :app:assembleAndroidTest -PfwBuildRust=true -PfwCargoPath=/opt/homebrew/opt/rustup/bin/cargo`、`./gradlew :framework:assembleRelease :app:assembleDebug -PfwBuildRust=true -PfwCargoPath=/opt/homebrew/opt/rustup/bin/cargo`、默认 `./gradlew :framework:assembleRelease :app:assembleDebug --rerun-tasks`、默认 C++ 回退 `connectedAndroidTest`、带 Rust `connectedAndroidTest` 均已通过。`android emulator start medium_phone` 首次受快照影响退出，已通过禁用快照的命令完成 Android 16 / arm64-v8a 模拟器验证。

## 10. 真实设备侧用例覆盖

| 测试类 | 用例数 | 覆盖范围 | 最终结果 |
|--------|--------|----------|----------|
| `FwStartInstrumentedTest` | 5 | `modeMask=0`、Application Context 稳定策略、Activity Context 稳定策略、系统限制策略受控结果、所有 `FwStartStrategy` 枚举覆盖 | 5 tests / 0 failures / 0 skipped |
| `FwRustNativeInstrumentedTest` | 3 | Rust `/proc` 进程快照、Rust MediaRoute 状态机、公共 MediaRoute Rust/C++ 后端状态迁移 | 3 tests / 0 failures / 0 skipped |

Debug 包新增 `TestStartActivity` 只用于 instrumentation 测试，避免示例 `MainActivity` 的 Compose 动画影响 `startActivitySync` 空闲等待；该 Activity 位于 debug 源集，不进入 release 产物。
